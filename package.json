{
  "name": "twogether",
  "version": "1.0.0",
  "private": true,
  "description": "Twogether — couples app (web + iOS via Capacitor)",
  "scripts": {
    "build:web": "mkdir -p www && cp index.html www/index.html",
    "sync": "npm run build:web && npx cap sync ios",
    "ios": "npm run sync && npx cap open ios"
  },
  "dependencies": {
    "@capacitor/core": "^7.0.0",
    "@capacitor/ios": "^7.0.0"
  },
  "devDependencies": {
    "@capacitor/assets": "^3.0.5",
    "@capacitor/cli": "^7.0.0"
  }
}
